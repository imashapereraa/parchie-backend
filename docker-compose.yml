services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: parchie
      POSTGRES_USER: parchie
      POSTGRES_PASSWORD: parchie
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
